---
import { categoryLabels, getProductPriceReview } from "../data/catalog";
import type { Product } from "../data/catalog";

interface Props {
  product: Product;
  useCase: string;
  secondaryUseCase: string;
  priceTier: string;
  title?: string;
}

const { product, useCase, secondaryUseCase, priceTier, title = `Buying details for ${product.title}` } = Astro.props;
const priceReview = getProductPriceReview(product);

const rows = [
  ["Brand", product.brand],
  ["Category", categoryLabels[product.category]],
  ["Current listed price", product.priceLabel],
  ["Price status", priceReview.label],
  ["Price tier", priceTier],
  ...(product.editorial ? [["Best fit", product.editorial.bestFor]] : []),
  ["Primary use", useCase],
  ["Secondary use", secondaryUseCase],
  ["Retailer title", product.retailerTitle ?? product.title],
  ["Price reference", priceReview.checkedAtLabel],
  ["Retailer path", product.affiliateMode === "exact" ? "Direct product listing" : "Amazon search results"],
  ["Tags", product.tags.join(", ")]
];
---

<article class="min-w-0 rounded-[1.75rem] border border-black/6 bg-white p-6 shadow-card">
  <p class="text-xs font-semibold uppercase tracking-[0.18em] text-accent-600">Specs and buying details</p>
  <h2 class="mt-3 font-serif text-3xl font-semibold tracking-tight text-ink-900">
    {title}
  </h2>
  <p class="mt-3 text-sm leading-7 text-ink-700">
    Keep the practical details together before checking the retailer listing.
  </p>
  <div class="mt-5 max-w-full overflow-x-auto">
    <table class="min-w-full text-left text-sm text-ink-700">
      <tbody>
        {
          rows.map(([label, value]) => (
            <tr class="border-b border-black/6 last:border-b-0">
              <th class="w-48 py-3 pr-4 align-top text-xs font-semibold uppercase tracking-[0.16em] text-ink-700/70">
                {label}
              </th>
              <td class="py-3 text-ink-900">{value}</td>
            </tr>
          ))
        }
      </tbody>
    </table>
  </div>
</article>
